import target from "../../src/shared/GetFilesChanged";

const testRegex = new RegExp(".*dist/.*");

describe.skip("get files changed", () => {
  it("should", () => {
    console.log("TBD");
  });
});
